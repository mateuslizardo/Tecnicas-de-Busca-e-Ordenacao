#include "item.h"

/*void sort(Item *a, int lo, int hi){
    for(int i=lo; i<hi; i++){
        int menor = a[i];
        for(int j=i+1; j<hi; j++){  
            if(less(a[j], menor)){
                menor = a[j];
            }
        }
        exch(a[i], menor);
    }
}*/